---
description: Finish the current change — verify, stage exactly, commit.
---

Follow the `## Done` section of `AGENTS.md`.

Argument (optional): `$ARGUMENTS` — a commit message or scope hint. If absent, derive both from the diff.
