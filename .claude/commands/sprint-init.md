---
description: "Initialize a new sprint from a plan — creates sprint/sprint.json with features and acceptance criteria."
---

# Command: /sprint-init

Create a new sprint from a plan, backlog, or user description. Generates `sprint/sprint.json` (v2 schema) and triggers `sprint/sprint.md` generation via hook.

## Usage

```
/sprint-init [plan or description]
```

## Instructions

### Step 0: Detect Agent Identity

Determine which agent you are:
- If context loaded from CLAUDE.md → agent = "cc"
- If context loaded from AGENTS.md → agent = "cx"
- Fallback: ask user

Store as `$AGENT_ID` for branch naming and sprint.json updates.

### Step 1: Read Project Context

Read `PROJECT.md` for project context (stack, patterns, domain).

### Step 2: Gather Sprint Input

- If `$ARGUMENTS` contains a plan or feature list: use that directly
- If `$ARGUMENTS` references a file (e.g., `BACKLOG.md`, `plan.md`): read that file
- If `$ARGUMENTS` is empty: ask user "Millised featuurid sellesse sprinti tulevad?"

### Step 3: Parse Features from Input

Extract distinct features from the plan. For each feature, generate:

| Field | Format | Description |
|-------|--------|-------------|
| `id` | `F001`, `F002`, ... | Sequential ID |
| `name` | Short English name | Used in branch names and commits |
| `description` | 1-2 sentences | What needs to be done |
| `acceptance_criteria` | List of strings | Concrete, testable criteria |
| `complexity` | `low` / `medium` / `high` | Scope estimate |
| `status` | `"pending"` | Always starts as pending |
| `assigned_to` | `null` | Assigned later via /feature or /handoff |
| `branch` | `null` | Set when feature is started |
| `tested` | `false` | Set to true by /done |
| `git` | `{ hash: null, message: null, timestamp: null }` | Populated by /done |
| `completed_at` | `null` | ISO timestamp when done |
| `review` | `{ score: null, verdict: null, reviewer: null }` | Populated by /peer-review |

### Step 4: Branching Strategy

Ask the user:

```
Kuidas tööd korraldada?
  1. main — mõlemad agendid töötavad samas repos, eraldi harudes (vaikimisi)
  2. worktree — teine agent saab eraldi worktree

Vali [1/2] (vaikimisi: 1):
```

- Default: `"main"`
- Save chosen strategy to `sprint.json` as `branch_strategy`
- Save current branch as `base_branch`

### Step 5: Determine Sprint ID

- If `sprint/sprint.json` already exists: increment sprint_id (S01 -> S02)
- If not: start with `S01`
- If existing sprint has incomplete features: warn user and ask for confirmation

### Step 6: Create Sprint Directory and File

- Create `sprint/` directory if it doesn't exist
- Write `sprint/sprint.json` with the v2 schema (see below)
- The `sprint/sprint.md` file is auto-generated by the sprint-sync hook -- do NOT create it manually

### Step 7: Display Sprint Summary

## Output: sprint/sprint.json (v2 schema)

```json
{
  "$schema": "sprint-v2",
  "sprint_id": "S01",
  "created": "2026-03-13T10:00:00Z",
  "branch_strategy": "main",
  "base_branch": "main",
  "current_feature": null,
  "features": [
    {
      "id": "F001",
      "name": "Feature name",
      "description": "What needs to be done",
      "acceptance_criteria": [
        "Criterion 1",
        "Criterion 2"
      ],
      "complexity": "medium",
      "status": "pending",
      "assigned_to": null,
      "branch": null,
      "tested": false,
      "git": {
        "hash": null,
        "message": null,
        "timestamp": null
      },
      "completed_at": null,
      "review": {
        "score": null,
        "verdict": null,
        "reviewer": null
      }
    }
  ],
  "stats": {
    "total": 5,
    "pending": 5,
    "in_progress": 0,
    "in_review": 0,
    "completed": 0
  },
  "last_updated": "2026-03-13T10:00:00Z",
  "last_updated_by": null
}
```

## Output Format (terminal)

```
╔══════════════════════════════════════════════════╗
║  Sprint S01 Initialized                          ║
╚══════════════════════════════════════════════════╝

Agent: $AGENT_ID
Strategy: main | base: main

Features (5):
  F001  [low]     User registration form
  F002  [medium]  Authentication API endpoints
  F003  [medium]  JWT token management
  F004  [high]    Role-based access control
  F005  [low]     Login page UI

Files created:
  + sprint/sprint.json
  ~ sprint/sprint.md (auto-generated by hook)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Next step: /feature to start working on F001
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

## Rules

- **Always read PROJECT.md** first for project context
- **Acceptance criteria must be testable** -- "works correctly" is not acceptable; "returns 200 with JWT token" is
- **Feature names in English** -- code-facing, used in commit messages and branch names
- **Descriptions can be bilingual** -- if user writes in Estonian, keep context but ensure names are English
- **Never overwrite** an active sprint without user confirmation
- **Complexity is an estimate** -- used by `/feature` for agent routing (low -> implementer, high -> planner -> architect -> implementer)
- **All features start unassigned** -- `assigned_to: null` until picked up via /feature or /handoff
- **Do not create sprint/progress.md** -- replaced by sprint/sprint.md which is auto-generated by hook

## Examples

### From a plan description
```
/sprint-init Vaja: kasutaja registreerimine, login API, JWT tokenid, rollipohine ligipääs, login UI

-> Sprint S01 Initialized (5 features, strategy: main)
```

### From a backlog file
```
/sprint-init Read features from BACKLOG.md

-> Sprint S01 Initialized (8 features from BACKLOG.md)
```

### New sprint when previous exists
```
/sprint-init Next sprint: performance optimeerimised, caching, monitoring

-> Warning: Sprint S01 exists with 2/5 incomplete features.
   Continue with new sprint S02? (incomplete features will be carried over)
```

### Empty -- interactive mode
```
/sprint-init

-> Millised featuurid sellesse sprinti tulevad?
  (Kirjelda vabas vormis, loen BACKLOG.md-st, voi anna feature'de nimekiri)
```

---

*Part of DG-VibeCoding-Framework v5.0.0*
